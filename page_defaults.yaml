# This file contains the default values for all metafiles.
# Other metafiles can override the contents of this one.

# Built-in
layout:       "default"
filename:     "index"
extension:    "html"
filters_pre:
  - erb
  - italicize_proper_names
filters_post:
  - rubypants_ddfreyne
  - relativize_paths_in_html

# Custom
